node_modules/
.env
.env.*
!.env.example
.DS_Store
Thumbs.db
*.log
.next/
dist/
build/

# Originais de placeholder: só a versão WebP (já convertida) vai pro repo,
# seguindo a mesma regra que o produto real vai seguir (sem guardar original).
placeholder/*.png
